public class Merge{

    public void mergeSort(int[]data){
	new int[] temp = data.clone();
	msort(data, temp, 0, data.length - 1);
    }

    private void msort(int[] data, int[] temp, int lo, int hi){
	if (lo >= hi) return data;
	
    }

    private void merge(int[] data, int[] temp){
	
    }
    
}
